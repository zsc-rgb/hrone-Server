# 🚀 第4阶段快速开始指南

> Redis缓存 - 实现高性能缓存功能

---

## ✅ 已完成配置

### 4.1 Redis配置 ✅

**已配置：**
- ✅ Redis依赖（spring-boot-starter-data-redis）
- ✅ 连接池依赖（commons-pool2）
- ✅ Fastjson2序列化（序列化器）
- ✅ RedisTemplate配置（Fastjson2序列化）
- ✅ application-dev.yml配置

**配置文件：**
- `application-dev.yml` - Redis连接配置
- `RedisConfig.java` - RedisTemplate和序列化器配置

---

### 4.2 RedisCache工具类 ✅

**已创建：**
- ✅ RedisCache - 缓存操作工具类
- ✅ String操作（set、get、delete等）
- ✅ List操作（列表增删改查）
- ✅ Hash操作（哈希表操作）
- ✅ Set操作（集合操作）
- ✅ 通用操作（keys、expire等）

**文件清单：**
```
hrone-common/
└── utils/
    └── RedisCache.java              ✅ Redis缓存工具类（500+行）

hrone-framework/
└── config/
    └── RedisConfig.java             ✅ Redis配置类（90行）

hrone-admin/
└── controller/
    └── RedisCacheTestController.java ✅ Redis测试接口（400+行）
```

---

## ⚠️ 重要：启动前准备

### 步骤1：确认Redis服务已启动

**检查Redis是否运行：**
```bash
redis-cli ping
```

**预期输出：** `PONG` ✅

**如果Redis未启动：**
```bash
# Windows（已安装服务）
net start Redis

# Windows（未安装服务）
redis-server

# Docker
docker start redis
```

---

### 步骤2：验证Redis连接

**测试连接：**
```bash
# 连接Redis
redis-cli

# 测试命令
ping
set test "hello"
get test
del test

# 退出
exit
```

**预期：** 所有命令正常执行 ✅

---

### 步骤3：修改Redis密码（如果需要）

**如果你的Redis设置了密码，请修改：**

文件：`hrone-admin/src/main/resources/application-dev.yml`

```yaml
spring:
  redis:
    password: your_password  # 修改为你的Redis密码
```

**注意：** 默认情况下Redis没有密码，保持为空即可。

---

### 步骤4：编译项目

```bash
cd d:\HroneWorking\hrone-Server
mvn clean install -DskipTests
```

**预期：** BUILD SUCCESS ✅

---

## 🧪 测试接口

### Redis缓存接口（10个）

| 序号 | 接口 | 方法 | 说明 |
|-----|------|------|------|
| 1 | `/test/redis/string` | GET | String基本操作 |
| 2 | `/test/redis/string` | DELETE | String删除操作 |
| 3 | `/test/redis/list` | GET | List列表操作 |
| 4 | `/test/redis/hash` | GET | Hash哈希表操作 |
| 5 | `/test/redis/set` | GET | Set集合操作 |
| 6 | `/test/redis/expire` | GET | 过期时间操作 |
| 7 | `/test/redis/keys` | GET | 模糊查询 |
| 8 | `/test/redis/batch` | DELETE | 批量删除 |
| 9 | `/test/redis/clean` | DELETE | 清理测试数据 |
| 10 | `/test/redis/object` | GET | 对象缓存 |

---

### 测试示例

#### 1. String基本操作

```
GET http://localhost:8080/test/redis/string

响应：
{
  "code": 200,
  "msg": "String操作测试成功",
  "data": {
    "name": "张三",
    "age": 25,
    "tempExpire": "10秒",
    "hasName": true,
    "counter": 5
  }
}
```

**测试了什么：**
- ✅ set() - 设置缓存
- ✅ get() - 获取缓存
- ✅ set(key, value, timeout) - 设置带过期时间的缓存
- ✅ hasKey() - 判断key是否存在
- ✅ increment() - 递增操作

---

#### 2. List列表操作

```
GET http://localhost:8080/test/redis/list

响应：
{
  "code": 200,
  "msg": "List操作测试成功",
  "data": {
    "users": ["管理员", "用户2", "用户3", "用户4", "用户5"],
    "size": 5,
    "updatedUsers": ["管理员", "用户2", "用户3", "用户4", "用户5"]
  }
}
```

**测试了什么：**
- ✅ listRightPush() - 添加元素到列表尾部
- ✅ listRightPushAll() - 批量添加元素
- ✅ getList() - 获取列表
- ✅ getListSize() - 获取列表长度
- ✅ listSet() - 修改列表元素

---

#### 3. Hash哈希表操作

```
GET http://localhost:8080/test/redis/hash

响应：
{
  "code": 200,
  "msg": "Hash操作测试成功",
  "data": {
    "name": "张三",
    "age": 25,
    "user2": {
      "name": "李四",
      "age": 30,
      "email": "lisi@hrone.com"
    },
    "keys": ["name", "age", "email"],
    "hasName": true
  }
}
```

**测试了什么：**
- ✅ hashPut() - 设置Hash字段
- ✅ hashGet() - 获取Hash字段
- ✅ hashPutAll() - 批量设置Hash
- ✅ hashGetAll() - 获取所有Hash数据
- ✅ hashKeys() - 获取所有Hash键
- ✅ hashHasKey() - 判断Hash键是否存在

---

#### 4. Set集合操作

```
GET http://localhost:8080/test/redis/set

响应：
{
  "code": 200,
  "msg": "Set操作测试成功",
  "data": {
    "tags": ["Java", "Go"],
    "size": 2,
    "hasJava": true,
    "hasCpp": false,
    "removed": 1,
    "afterRemove": ["Java", "Go"]
  }
}
```

**测试了什么：**
- ✅ setAdd() - 添加元素到集合
- ✅ getSet() - 获取集合所有元素
- ✅ getSetSize() - 获取集合大小
- ✅ setHasValue() - 判断元素是否存在
- ✅ setRemove() - 移除元素

---

#### 5. 过期时间操作

```
GET http://localhost:8080/test/redis/expire

响应：
{
  "code": 200,
  "msg": "过期时间测试成功",
  "data": {
    "expire": "60秒",
    "expire2": "300秒",
    "expire3": "30秒"
  }
}
```

**测试了什么：**
- ✅ expire(key, timeout) - 设置过期时间（秒）
- ✅ expire(key, timeout, unit) - 设置过期时间（指定单位）
- ✅ getExpire() - 获取剩余过期时间

---

#### 6. 模糊查询

```
GET http://localhost:8080/test/redis/keys

响应：
{
  "code": 200,
  "msg": "模糊查询测试成功",
  "data": {
    "stringKeys": [
      "test:string:name",
      "test:string:age",
      "test:string:counter"
    ],
    "hashKeys": [
      "test:hash:user:1",
      "test:hash:user:2"
    ],
    "allKeys": [...所有test开头的key...],
    "totalCount": 15
  }
}
```

**测试了什么：**
- ✅ keys(pattern) - 查询匹配模式的所有key

---

#### 7. 对象缓存

```
GET http://localhost:8080/test/redis/object

响应：
{
  "code": 200,
  "msg": "对象缓存测试成功",
  "data": {
    "user": {
      "id": 1,
      "name": "张三",
      "age": 25,
      "email": "zhangsan@hrone.com"
    },
    "users": [
      { "id": 1, "name": "用户1", "age": 21, "email": "user1@hrone.com" },
      { "id": 2, "name": "用户2", "age": 22, "email": "user2@hrone.com" },
      { "id": 3, "name": "用户3", "age": 23, "email": "user3@hrone.com" }
    ]
  }
}
```

**测试了什么：**
- ✅ 缓存Java对象（自动序列化）
- ✅ 缓存List集合（自动序列化）
- ✅ Fastjson2序列化和反序列化

---

#### 8. 清理测试数据

```
DELETE http://localhost:8080/test/redis/clean

响应：
{
  "code": 200,
  "msg": "清理完成，共删除15个key"
}
```

**测试了什么：**
- ✅ deleteKeys(pattern) - 批量删除匹配的key

---

## 🔍 核心知识点

### 1. RedisTemplate配置

**序列化器选择：**
```java
// 使用Fastjson2序列化（推荐）
- 优点：性能好、支持复杂对象、保留类型信息
- 缺点：依赖Fastjson库

// Key使用String序列化
- 优点：Redis中直接可读
- 缺点：无

// Value使用Fastjson2序列化
- 优点：支持对象、性能好
- 缺点：Redis中看到的是JSON
```

---

### 2. Redis数据类型

**String - 字符串：**
- 最基本的类型，可以存储字符串、数字、对象（序列化）
- 场景：缓存、计数器、分布式锁

**List - 列表：**
- 有序的字符串列表
- 场景：消息队列、最新消息列表

**Hash - 哈希表：**
- 键值对集合，适合存储对象
- 场景：用户信息、商品信息

**Set - 集合：**
- 无序、不重复的元素集合
- 场景：标签、好友列表、去重

**ZSet - 有序集合：**
- 有序、不重复的元素集合
- 场景：排行榜、延时队列（第4阶段不涉及）

---

### 3. 过期策略

**设置过期时间的方式：**
```java
// 方式1：设置缓存时指定
redisCache.set("key", "value", 60);  // 60秒后过期

// 方式2：设置缓存后再设置过期时间
redisCache.set("key", "value");
redisCache.expire("key", 60);

// 方式3：指定时间单位
redisCache.set("key", "value", 5, TimeUnit.MINUTES);
```

**过期时间说明：**
- 单位：秒（默认）
- -1：永不过期
- -2：key不存在
- 0：立即过期

---

### 4. 缓存应用场景

**1. 热点数据缓存**
```java
// 缓存用户信息（避免频繁查询数据库）
User user = redisCache.get("user:" + userId);
if (user == null) {
    user = userService.getById(userId);
    redisCache.set("user:" + userId, user, 3600);
}
```

**2. 计数器**
```java
// 文章浏览次数
redisCache.increment("article:view:" + articleId, 1);
```

**3. 分布式锁**
```java
// 设置锁（NX表示不存在时才设置）
Boolean locked = redisTemplate.opsForValue()
    .setIfAbsent("lock:order:" + orderId, "1", 10, TimeUnit.SECONDS);
```

**4. 会话管理**
```java
// 存储用户登录信息
redisCache.set("token:" + token, loginUser, 1800);
```

---

## 📊 Redis监控

### 使用redis-cli监控

```bash
# 查看Redis信息
redis-cli info

# 实时监控命令
redis-cli monitor

# 查看所有key
redis-cli keys *

# 查看key数量
redis-cli dbsize

# 查看内存使用
redis-cli info memory
```

### 使用客户端工具

推荐使用 **Another Redis Desktop Manager**：
- 可视化查看所有key
- 实时监控Redis状态
- 方便的数据管理

---

## ⚡ 启动项目

### 方法1：使用启动脚本

```bash
双击：d:\HroneWorking\启动后端.bat
```

**启动成功标志：**
```
Lettuce PoolingConnectionProvider.getConnection
========================================
   HROne 后端系统启动成功！
========================================
```

### 方法2：命令行启动

```bash
cd d:\HroneWorking\hrone-Server
mvn spring-boot:run -pl hrone-admin
```

---

## 🆘 常见问题

### Q1: 启动时报Redis连接失败？

**错误：** `Unable to connect to Redis`

**检查：**
1. Redis服务是否启动：`redis-cli ping`
2. 端口是否正确：默认6379
3. 密码是否正确：检查配置文件

**解决：**
```bash
# 启动Redis
redis-server

# 或者启动服务
net start Redis
```

---

### Q2: 序列化异常？

**错误：** `SerializationException`

**原因：** 对象没有无参构造器或字段不可序列化

**解决：**
1. 确保对象有无参构造器
2. 字段类型支持序列化
3. 使用Fastjson2的注解（如需要）

---

### Q3: 中文乱码？

**问题：** Redis中查看数据是乱码

**原因：** 使用了序列化器，数据被序列化了

**说明：** 这是正常的！
- Fastjson2序列化后会保存为JSON格式
- 在Java中反序列化后就是正常的
- 如果想在Redis中看到原文，可以使用String存储

---

### Q4: 内存溢出？

**错误：** `OutOfMemoryError`

**原因：** 缓存了大量数据且没有设置过期时间

**解决：**
1. 给缓存设置过期时间
2. 定期清理不用的缓存
3. 调整Redis最大内存配置

---

## ✅ 完成检查清单

开始测试前，请确认：

### Redis准备 ✅
- [ ] Redis已安装并启动
- [ ] 可以使用redis-cli连接
- [ ] 基本命令可以执行
- [ ] （可选）客户端工具已安装

### 项目配置 ✅
- [x] Redis依赖已添加
- [x] application-dev.yml已配置
- [x] RedisConfig已创建
- [x] RedisCache已创建
- [x] 测试接口已创建

### 代码实现 ✅
- [x] RedisTemplate配置完成
- [x] Fastjson2序列化器配置完成
- [x] RedisCache工具类完成
- [x] 测试Controller完成
- [x] 项目编译成功

---

## 🎯 下一步

**准备工作完成后：**
1. 启动Redis服务
2. 启动项目
3. 测试Redis缓存接口
4. 查看Redis中的数据

**详细测试文档：** 本文档已包含所有测试场景

---

**🎉 第4阶段代码已就绪！等待Redis准备完成即可测试！** 🚀

