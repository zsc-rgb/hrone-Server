# 🚀 快速开始

## 第1步：环境检查

确保已安装以下环境：

- ✅ **JDK 1.8+**
  ```bash
  java -version
  ```

- ✅ **Maven 3.6+**
  ```bash
  mvn -version
  ```

- ✅ **IDEA**（推荐）或其他Java IDE

## 第2步：导入项目

### 使用IDEA导入

1. 打开IDEA
2. 选择 `File` → `Open`
3. 选择 `hrone-Server` 文件夹
4. 等待Maven自动下载依赖（首次较慢，请耐心等待）

### 使用命令行编译

```bash
cd hrone-Server
mvn clean install
```

## 第3步：启动项目

### 方式1：IDEA中启动

1. 找到 `hrone-admin/src/main/java/com/hrone/HROneApplication.java`
2. 右键 → `Run 'HROneApplication'`
3. 看到以下输出说明启动成功：

```
  _   _ ____   ___             
 | | | |  _ \ / _ \ _ __   ___ 
 | |_| | |_) | | | | '_ \ / _ \
 |  _  |  _ <| |_| | | | |  __/
 |_| |_|_| \_\___/|_| |_|\___|

========================================
   HROne 后端系统启动成功！
   访问地址：http://localhost:8080
   测试接口：http://localhost:8080/test/hello
========================================
```

### 方式2：命令行启动

```bash
cd hrone-Server
mvn spring-boot:run -pl hrone-admin
```

## 第4步：测试接口

### 测试Hello接口

打开浏览器访问：http://localhost:8080/test/hello

应该看到：
```json
{
  "code": 200,
  "msg": "Hello, HROne!",
  "data": "项目启动成功！",
  "time": "2025-11-03T..."
}
```

### 测试健康检查接口

访问：http://localhost:8080/test/health

应该看到：
```json
{
  "status": "UP",
  "message": "服务运行正常",
  "timestamp": 1730678400000
}
```

## ✅ 启动成功！

恭喜您成功启动了第一个模块！

## 📚 下一步

现在您已经完成了 **第1阶段：基础搭建**

接下来请阅读：
- `docs/phase-01-基础搭建.md` - 了解项目结构和知识点
- `docs/phase-02-通用工具.md` - 开始第2阶段学习

## ❓ 常见问题

### Q1: Maven下载依赖很慢怎么办？

**A:** 配置阿里云Maven镜像（项目已配置）

如果还是慢，可以修改 `~/.m2/settings.xml`：
```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>*</mirrorOf>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

### Q2: 启动报错：找不到或无法加载主类

**A:** 
1. 在IDEA中：`File` → `Invalidate Caches` → 重启IDEA
2. 删除 `target` 目录，重新编译

### Q3: 端口8080被占用

**A:** 修改 `application.yml` 中的端口号：
```yaml
server:
  port: 8081  # 改成其他端口
```

### Q4: IDEA没有识别为Maven项目

**A:** 
1. 右键项目根目录的 `pom.xml`
2. 选择 `Add as Maven Project`

## 💡 提示

- 建议使用IDEA进行开发，体验最好
- 开启 `Enable auto-import` 自动导入Maven依赖
- 安装Lombok插件（`Settings` → `Plugins` → 搜索`Lombok`）
- 使用快捷键 `Ctrl + F9` 快速编译
- 使用快捷键 `Shift + F10` 快速运行

---

祝您学习愉快！💪

